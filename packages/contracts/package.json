{
  "name": "@repo/contracts",
  "type": "module",
  "scripts": {
    "generate-abis": "tsx generate-abis.ts",
    "clean": "forge clean",
    "build": "pnpm clean && forge build && pnpm generate-abis",
    "test": "forge test"
  },
  "exports": {
    "./abi": "./src/abi/index.ts"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "tsx": "^4.21.0"
  }
}